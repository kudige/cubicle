# Cubicle implementation plan

## Completed

- Documented the initial architecture in docs/architecture.md.
- Added buildable manager and controller binaries.
- Added shared process mode and state types.
- Implemented process mode/state name helpers.
- Added CTest-based test wiring.
- Added a process helper unit test.
- Added common utility unit tests:
  - generated hex ID shape and buffer validation
  - recursive directory creation and idempotence
  - full-buffer writes through a pipe
- Added manager registry parser unit tests:
  - workspace record parsing
  - process record parsing
  - cursor record parsing
  - primitive event sequence parsing
- Added opt-in gcov/lcov coverage reporting:
  - CUBICLE_ENABLE_COVERAGE CMake option
  - coverage-summary build target
  - test sources filtered from production coverage summary
- Implemented controller stream mode:
  - fork/exec command launch
  - independent child process group
  - stdin/stdout/stderr pipe wiring
  - EOF stdin until attachments exist
  - stdout/stderr mirroring to the invoking terminal
  - child exit status propagation
- Implemented controller TTY mode:
  - allocate a PTY for stdin/stdout/stderr
  - expose combined terminal output through the existing stdout stream
  - forward foreground terminal stdin to the PTY in raw mode
  - propagate initial and changed terminal window size
  - notify the managed process group after TTY window-size changes
  - preserve socket stdin attachment semantics for interactive TTY input
  - support TTY resize over the control socket
- Added initial durable controller state for stream mode:
  - metadata
  - stdout.log
  - stderr.log
  - sequence-numbered events.log
- Added a controller stream integration test covering output capture, event
  recording, metadata, and child exit status.
- Added a controller TTY integration test covering PTY terminal detection,
  combined output capture, metadata, and exit events.
- Added a controller foreground TTY integration test covering local stdin
  forwarding and SIGWINCH resize propagation.
- Added a controller TTY socket integration test covering stdin attachment and
  resize commands.
- Added a controller-local control socket for stream mode:
  - status queries
  - stdout/stderr range reads from persisted logs
  - terminate requests
  - signal requests
  - signal-delivered primitive events
- Hardened the controller-local control socket:
  - nonblocking listener
  - nonblocking request reads
  - incomplete client requests do not block the controller
  - client write failures do not bring down the controller
  - SIGPIPE is ignored so socket disconnects are ordinary write failures
- Added basic stream attachment semantics:
  - attach stdout/stderr from a persisted offset
  - catch up from persisted logs
  - live follow newly captured output
  - close attachments when the process exits
  - client attached/detached primitive events
- Added stdin attachment for stream mode:
  - attach stdin/in over the control socket
  - forward attached bytes into child stdin
  - record input primitive events
  - support stdin attachment in the Python control socket client
  - Python control socket client can detect controller mode from metadata
  - Python control socket client enables raw terminal input for TTY stdin attachment
  - Python control socket client supports attach tty for bidirectional TTY attachment
  - attach tty defaults to the current output offset for live reattachment
  - attach tty reasserts the active client terminal size without blocking each input chunk
  - oversized explicit output attach catch-up requests return read_too_large
  - terminal_event_recorder.py records TTY input and resize events for E2E tests
  - terminal_resize_e2e_test.py covers foreground+socket and socket+socket resize flows
  - foreground controller input reasserts its terminal size after socket resize
  - controller tracks PTY size and skips unchanged resize requests
- Defined initial stream backpressure behavior:
  - nonblocking attachment writes fail closed
  - slow output clients are detached instead of silently losing bytes
  - stdin clients are detached if their bytes cannot be forwarded
- Documented the current local controller protocol:
  - command syntax
  - response schemas
  - stream read and attach framing
  - current error codes
  - primitive event shapes
- Added explicit stream stdin policy:
  - default --stdin-policy open for stdin attachments
  - --stdin-policy eof for immediate child stdin EOF
  - stdin_policy persisted in metadata
  - attach stdin returns error stdin_closed when stdin is closed
- Hardened default controller state identity:
  - generated controller IDs
  - default state directories use controller IDs instead of child PIDs
  - controller_id persisted in metadata
  - process_started events include controller_id
- Hardened primitive event writes:
  - events.log is opened with append semantics
  - each event record is emitted with one append write
- Added controller recovery read paths:
  - metadata command returns raw metadata through the control socket
  - events after SEQUENCE LIMIT returns primitive events after a cursor
  - Python control socket client supports metadata and events-after
- Defined current event and metadata formats in docs/protocol.txt.
- Added controller daemon mode:
  - --daemon option
  - detach before launching the managed process
  - redirect controller stdin/stdout/stderr to /dev/null
  - keep interaction available through the control socket and persisted logs
- Added the first manager registry slice:
  - workspace records
  - generated stable workspace IDs
  - generated stable process IDs
  - workspace-local friendly names in process records
  - process/controller mapping records
  - process list filtering by workspace name or ID
- Added initial manager/controller routing:
  - process start launches stream controllers as daemons
  - manager allocates stable process IDs before launch
  - controller state is stored under manager state by process ID
  - manager waits for controller socket and metadata
  - manager registers controller ID and direct control socket
  - process resolve returns direct controller details by process ID
  - process resolve supports workspace-local friendly names
  - process I/O remains direct through the controller socket
  - short-lived processes that exit before socket discovery are registered as exited
- Added initial workspace event aggregation:
  - events poll consumes manager-started controller primitive journals
  - manager maintains per-process cursors
  - manager appends workspace-scoped events
  - events can be filtered by workspace name or ID
  - events list reads persisted workspace events
  - events follow performs bounded repeated polling
- Improved the manager event loop:
  - events follow runs until interrupted when --iterations is omitted
  - bounded --iterations mode remains available for tests and scripts
  - follow flushes emitted workspace events after each poll iteration
- Hardened manager persistence in the current CLI architecture:
  - mutating manager commands take an exclusive advisory state lock
  - process IDs are enforced as globally unique
  - process friendly names are enforced as workspace-local unique
- Added a controller control-socket integration test.
- Added a controller attachment integration test.
- Added a controller stdin attachment integration test.
- Added a controller daemon integration test.
- Added a manager registry integration test.
- Added a manager process-start integration test.
- Extended the manager process-start integration test to cover TTY mode.
- Split the controller implementation into focused internal modules:
  - CLI entrypoint
  - stream process lifecycle
  - durable state and event journaling
  - control socket protocol and attachments
  - shared controller I/O helpers
- Hardened controller command-line protocol parsing:
  - accept LF and CRLF request terminators
  - reject embedded NUL bytes as bad_request
  - document ASCII command tokens with raw byte response payloads
- Hardened controller stream lifecycle:
  - keep the control socket reachable after stdout/stderr EOF while the child is alive
  - reap child exit from the stream loop without losing the recorded exit status
  - regress output-EOF-before-child-exit control socket behavior
- Hardened controller state initialization failure paths:
  - initialize controller state before failure cleanup can use it
  - separate child exit status calculation from durable exit-event recording
  - avoid exit-event writes when the event journal was not opened
- Added controller client-failure isolation regressions:
  - oversized requests fail only that client
  - malformed attach requests fail only that client
  - later status and terminate requests continue to work
- Hardened controller state directory creation:
  - create parent directories and allow an existing empty leaf state directory
  - fail normal launch instead of truncating reused state
  - regress reused state directory preservation
- Hardened controller control socket creation:
  - do not unlink live socket paths
  - do not unlink non-socket filesystem entries
  - replace stale socket paths only
  - set control socket permissions explicitly
- Finished the current stream control response semantics:
  - queue bounded per-client control responses
  - flush queued responses from the stream poll loop with POLLOUT
  - cover slow large-read clients while other control requests continue
- Added opt-in controller completed-state retention:
  - --completed-retention-ms N keeps the control socket available after exit
  - completed status includes the child result and final stream offsets
  - read-only status, metadata, events, and stream reads remain available
  - interactive commands fail with process_completed
- Merged the draft transport-neutral API specification:
  - docs/api-spec-v0.md defines manager/controller semantic APIs
  - manager and controller APIs are specified independently of wire encoding
  - Unix sockets are the initial implementation transport
- Started the shared API implementation:
  - added protocol version and error-code definitions
  - added the transport vtable contract
  - added endpoint parsing for initial Unix socket endpoints
  - added a reusable Python API test client for framed JSON RPC over Unix sockets
  - refactored the manager daemon E2E test to use the shared API client
  - added a production libcubicle-to-real-manager Unix socket E2E test
  - real manager E2E currently tests:
    - manager.ping
    - manager.status
    - workspace.create
    - workspace.get
    - workspace.list
    - workspace.rename
    - workspace.stop
    - workspace.delete
    - workspace.key.add
    - workspace.key.list
    - workspace.key.update
    - workspace.key.revoke
    - process.start
    - process.get by process ID
    - process.get by workspace-local friendly name
    - process.list
    - process.signal
    - process.terminate
    - process.kill
    - process.wait
    - process.remove
    - process.read_output
    - attachment.request
    - events.list
    - events.subscribe
    - manager.shutdown
- Added framed controller API requests over the existing control socket:
  - 4-byte length-prefixed JSON RPC requests
  - controller.status
  - controller.attach
  - controller.read / controller.read_output
  - controller.write
  - controller.resize
  - controller.detach
  - controller.signal
  - controller.terminate
  - controller.events_after
  - legacy line protocol remains available on the same socket
- Added manager process lifecycle API endpoints:
  - process.kill routes SIGKILL to the owning controller
  - process.wait polls controller state and falls back to controller event logs
  - process.remove rejects live processes and removes retained manager/controller state for terminal processes
- Added manager workspace mutation API endpoints:
  - workspace.rename rewrites daemon-owned workspace state
  - workspace.stop terminates live processes in the workspace
  - workspace.delete removes workspace records and optionally retained process/controller state
  - workspace.key.update rewrites stored key capabilities
- Added initial events.subscribe support:
  - manager validates subscription filters and returns a subscription descriptor
  - libcubicle maintains a polling subscription cursor over events.list
  - real manager E2E consumes a subscribed event with cubicle_events_next
- Added the initial persistent manager daemon lifecycle:
  - daemon command runs a long-lived foreground manager process
  - Unix socket lifecycle supports ping, status, and shutdown
  - manager ID is persisted in manager state
  - stale manager socket paths are replaced safely
- Hardened the persistent manager daemon:
  - daemon uses a single-instance advisory lock
  - daemon socket accept loop is nonblocking
  - daemon owns periodic workspace event aggregation
  - daemon advances workspace event cursors without requiring foreground poll
- Hardened protocol JSON parsing:
  - replaced protocol JSON scanning with vendored yyjson parsing
  - added parser resource limits for document size, depth, object members, arrays, strings, methods, argv, and env arrays
  - reject duplicate object keys at every object level
  - reject ambiguous integer forms for unsigned protocol fields
  - reject malformed strings, invalid UTF-8, and isolated Unicode surrogates through the strict parser
- Added typed RPC envelope validation:
  - requests validate protocol major/minor, request IDs, method names, params objects, and top-level fields
  - responses validate request correlation, success/result/error combinations, and top-level fields
  - client response handling now consumes the parsed response envelope
- Added checked JSON decoder helpers:
  - required and optional string/u64/bool/object/array accessors
  - null rejection for optional fields unless explicitly handled
  - ASCII identifier validation for protocol identities
  - structured validation error details with field path, expected type, and message
- Converted client response decoders to checked JSON accessors:
  - endpoint, page, workspace, process, key, and event result decoding now reject present-but-invalid optional fields
  - unknown process modes/states and event types fail decoding instead of silently defaulting
- Converted implemented manager daemon API params to parsed typed accessors:
  - workspace.create
  - workspace.get
  - process.get
  - process.list
  - events.list
  - process.read_output
  - real manager E2E tests now cover invalid typed params over the Unix socket
- Hardened structured RPC error decoding:
  - shared parsed error-object decoder
  - public RPC error helper rejects success responses, result/error conflicts, null errors, invalid field types, and errno overflow
  - libcubicle client uses parsed envelope errors instead of ad hoc field reads
- Added protocol sanitizer and fuzz smoke support:
  - opt-in ASan/UBSan CMake build flag
  - fuzz-json-parser target
  - fuzz-rpc-envelope target
  - standalone fuzz smoke mode for environments without libFuzzer linkage
- Updated README.md with current build, test, and controller usage notes.
- Merged the user-facing cube CLI specification in docs/cli-command-spec.md.
- Added the initial cube CLI scaffold:
  - cube executable target
  - top-level help
  - global --manager-socket, --workspace, and --json parsing
  - manager discovery through --manager-socket or CUBICLE_MANAGER_SOCKET
  - clear diagnostics for missing manager sockets and unknown commands
- Added initial cube workspace commands over the manager API:
  - cube workspace
  - cube workspace NAME
  - cube workspace list
  - cube workspace create NAME
  - cube workspace select NAME
  - cube workspace stop NAME
  - cube workspace delete NAME
  - selected workspace persistence under XDG state or user-local state fallback
- Added initial cube process inspection commands over the manager API:
  - cube ps
  - cube inspect NAME
  - selected workspace and --workspace resolution
  - human-readable and --json output
  - daemon-backed E2E coverage for process.list and process.get
- Added initial cube process lifecycle commands over the manager API:
  - cube signal NAME SIGNAL
  - cube stop NAME
  - cube kill NAME
  - cube remove NAME
  - workspace-local process name resolution before process lifecycle calls
  - daemon-backed E2E coverage using real controller-backed processes
- Updated the cube launch command shape:
  - docs/cli-command-spec.md now uses cube run [OPTIONS] COMMAND [ARG...]
  - top-level cube COMMAND shortcut is reserved for a later phase
- Added initial cube run implementation over the manager API:
  - cube run [--fg|--bg] [--stream|--tty|--term] [--name NAME] COMMAND [ARG...]
  - foreground stream fallback waits and drains retained stdout/stderr
  - background launch returns startup metadata or human-readable status
  - generated process names from command basenames
  - daemon-backed E2E coverage for foreground, background, JSON, and TTY launch
- Fixed cube run launch edge cases:
  - generated friendly-name collisions retry with numeric suffixes like name-1
  - foreground TTY launch fails fast until attachment is implemented
  - regression coverage ensures a foreground TTY launch attempt does not wedge later cube commands
- Hardened manager storage diagnostics:
  - state-file write failures include the operation and path in manager logs
  - event aggregation write failures no longer tear down the daemon
  - cube transport read failures include the manager socket path and errno text
- Added initial cube logs/events commands:
  - cube logs NAME reads retained stdout/stderr for stream processes
  - cube logs NAME reads retained tty output for TTY processes
  - cube events lists retained workspace events through the manager API
  - cube --json events prints the raw API result for scripting
  - follow modes fail fast until subscription-backed CLI loops are implemented
- Added cube workspace JSON output contracts:
  - cube --json workspace reports the selected workspace name
  - cube --json workspace create/select/list/stop/delete return manager API results
- Covered cube process lifecycle JSON output contracts:
  - cube --json signal/stop/kill/remove return manager API results
- Added initial spec-backed cube connect:
  - cube connect NAME requests a manager attachment grant
  - cube connect uses controller.attach/controller.read/controller.write/controller.resize
  - stream-mode interactive and read-only connects have daemon-backed E2E coverage
  - controller.attach validates channels/mode, normalizes accepted channels, and records attachment events
  - controller.read supports tty as the retained PTY byte stream
  - manager/controller RPC success envelopes handle larger retained event/output responses
- Fixed stream connect terminal rendering:
  - stream-mode cube connect no longer puts the local terminal in raw mode
  - pseudo-terminal regression coverage ensures newline output returns to column zero
- Fixed process list state after lifecycle actions:
  - process.get and process.list synthesize terminal state from controller status/event logs
  - regression coverage verifies cube ps reports a killed process as completed
- Continued TTY attachment implementation:
  - foreground cube run --tty now starts the managed process and immediately attaches through a manager-issued grant
  - pseudo-terminal coverage verifies foreground TTY input reaches the child and exits cleanly
- Added workspace-scoped retained process cleanup:
  - manager.cleanup removes terminal retained processes and skips live ones
  - cube cleanup exposes the endpoint for the selected workspace
  - libcubicle exposes cubicle_manager_cleanup with removed/skipped/failed counts
- Added Ubuntu runtime packaging:
  - CMake install rules include cube, cubicle-manager, and cubicle-controller
  - CPack can build a Debian package with cmake --build build --target package
  - CMake can build a pacman package with cmake --build build --target pacman-package
  - .cubicle/ repo-local runtime state is ignored
- Hardened manager restart reconciliation:
  - daemon startup checks registered non-terminal controllers for responsiveness
  - unreachable controllers with recorded exit events are persisted as completed
  - unreachable controllers without exit evidence are persisted as lost
  - responsive controllers remain live
  - manager.reconcile exposes the same persistence pass through the API
- Added cube follow modes:
  - cube logs --follow tails retained stdout/stderr or TTY output until process completion
  - cube events --follow polls manager events until interrupted
  - cube events --follow --iterations N provides bounded follow mode for tests and scripts
- Implemented term mode:
  - controller --mode term wires stdin/stdout through a PTY
  - controller --mode term captures stderr through a separate pipe
  - term metadata and process_started events persist mode=term
  - manager process start and framed process.start accept term
  - cube run --term, cube connect, and cube logs handle terminal output with separately retained stderr
  - controller, manager, and cube E2E tests cover term stdout/stderr separation
  - background TTY controllers answer common startup terminal queries until an interactive attach is active
- Started configuration deployment support:
  - added libeconf-backed read-only configuration loading
  - packaged defaults define state_dir, runtime_dir, log_dir, listen socket, controller binary, and client manager endpoint
  - manager can split persistent state, runtime sockets, and controller logs
  - cube can use configured manager endpoint and run defaults
  - cube config show, paths, and validate expose read-only diagnostics
  - non-root defaults use per-user XDG state/runtime/log paths and socket
  - root and explicit system configuration still support shared system-manager paths
  - main user configuration loads from $XDG_CONFIG_HOME/cubicle/config.cfg or ~/.config/cubicle/config.cfg
- Added explicit insecure TCP manager listener support:
  - manager daemon accepts tcp://host:port endpoints only with --allow-insecure
  - tests/api_client.py can exercise Unix paths, unix:// endpoints, and tcp:// endpoints
  - CTest covers TCP refusal without --allow-insecure and ping/status/shutdown with it enabled
- Started authentication/session implementation from docs/auth-session-spec.md:
  - added OpenSSL-backed Ed25519 identity primitives and HMAC helper
  - manager now creates and preserves a strict-permission identity under state/keys
  - tests cover key generation, persistence, signatures, HMAC, and unsafe key permissions
  - added deterministic binary auth transcript and resume-authenticator encoders
  - added Unix-socket auth.challenge/auth.authenticate flow with signed transcript verification
  - added E2E coverage for signed local session creation over a persistent framed socket
  - libcubicle now generates/loads a client identity and uses signed Unix authentication when supported
  - cube now uses signed Unix manager authentication before issuing manager RPCs
  - manager daemon now stores authenticated sessions and verifies auth.resume HMACs
  - libcubicle now caches Unix manager sessions in XDG_RUNTIME_DIR and resumes them with auth.resume
  - cube now caches Unix manager sessions in XDG_RUNTIME_DIR and resumes them with auth.resume
  - manager applies workspace capability checks for workspace, process, event, attachment, and key-management APIs
  - workspace.create bootstraps the authenticated same-UID caller key as workspace owner
  - cube access add/list/set-role/remove manages workspace public-key access
  - manager Unix sockets support configurable socket_mode and socket_group
- Refactored shared client/UI code:
  - introduced libcubicle URI connection support used by cube and desk
  - introduced internal cubeui helpers for local endpoint, selected-workspace, and terminal utilities
  - switched desk to libcubicle for manager connections and cubeui for local terminal helpers
  - moved cube manager RPC transport/session/auth handling through libcubicle
  - added typed libcubicle attachment helpers for request, connect, read, write, resize, status, detach, channels, and replay
  - switched cube connect and foreground TTY run attachment paths to libcubicle attachment APIs
  - removed cube's private direct controller socket/RPC/auth implementation
- Added terminal snapshot support:
  - introduced a libvterm-backed internal terminal model
  - controller now maintains virtual terminal state for TTY and term stdout
  - added controller.snapshot for current screen cells, cursor, and offset
  - added libcubicle terminal snapshot types and attachment helper
  - desk initializes attached panes from controller snapshots and reads live output from the snapshot offset
  - cube connect initializes terminal attachments from snapshots before live output, falling back to bounded replay only when snapshots are unavailable
  - desk filters local terminal response sequences before forwarding input to cubes
  - added controller.debug=input to log escaped and hex input bytes for debugging
  - package/build dependencies now include libvterm
- Hardened desk snapshot/live handoff:
  - created fix/desk-libvterm-streams and fast-forwarded the fixes into main
  - controller uses libvterm-generated terminal responses for supported queries while retaining controller-only fallbacks for unsupported startup queries
  - desk no longer generates terminal query responses from its local render cache
  - libcubicle exposes attachment read offsets for diagnostics and regression tests
  - attachment snapshots explicitly advance the TTY read offset to the snapshot offset
  - desk regression coverage verifies large settled backlogs are not replayed after snapshot restore
  - attachment reads cap raw controller read requests so decoded JSON string expansion cannot overflow the caller buffer
  - attachment regression coverage verifies expanded JSON string data no longer causes invalid read data
- Continued configuration implementation:
  - added per-key effective configuration origin metadata
  - tracked built-in, system, user, and override source kinds
  - added public config origin/key/source-name helpers
  - added cube config effective to print each value and its source
  - added config unit/runtime/help coverage for origin reporting
  - added lexical config.cfg.d/*.cfg drop-in loading for system, user, and explicit override configs
  - config effective now lists distinct effective sources, including drop-in files
  - added config unit/runtime coverage for drop-in precedence and source reporting
  - implemented cube defaults show/set/reset for launch, mode, and kill-cleanup
  - cube defaults edits the user config atomically and preserves unrelated settings
  - added CLI help/runtime coverage for defaults updates

## Next

1. Continue configuration implementation from docs/configuration-spec.md.
   - Harden ownership and permission policy for configured directories.

2. Continue implementing the cube CLI specification.
   - Harden post-exit connect draining through manager-retained output fallback.
   - Add defaults config and command-rule phases from docs/cli-command-spec.md.
   - Keep cube talking to the controller only for authorized process attachments.

3. Continue JSON protocol hardening.
     - replace remaining protocol-facing textual JSON result extraction in libcubicle compatibility/client helpers
     - introduce a structured writer or yyjson-backed writer wrapper for responses and requests currently assembled by snprintf
     - add method schemas and malicious/boundary fixtures under schemas/v0 and tests/fixtures
     - add additional fuzz targets for process.start, process info, attachment grants, events, and RPC errors
     - define deterministic non-JSON signed transcript/grant encoding before authentication grants are implemented
     - make sanitizer and malformed-message runs mandatory in CI

4. Continue authentication/session implementation from docs/auth-session-spec.md.
   - add cross-UID/group E2E coverage with a second local user where available
   - implement secure TCP authentication and authorization
   - invalidate or reject resumed sessions after workspace key revocation
   - add audit records for workspace key changes

5. Expand tests.
   - Large output tests.
   - Interleaved stdout/stderr event tests.
   - Restart/recovery tests.
   - Broader PTY behavior tests for alternate screen, wide characters, OSC, mouse modes, and snapshot/live handoff.
